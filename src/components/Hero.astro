---
import { profile, metrics } from '../data/profile';
---

<section class="hero">
  <div class="wrap hero-inner">
    <p class="eyebrow">
      <span class="pulse" aria-hidden="true"></span>
      {profile.role} · {profile.nameEn}
    </p>

    <h1>{profile.headline}</h1>
    <p class="lede">{profile.summary}</p>

    <ul class="metrics" aria-label="핵심 지표">
      {
        metrics.map((m) => (
          <li>
            <span class="value">{m.value}</span>
            <span class="label">{m.label}</span>
          </li>
        ))
      }
    </ul>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-block: calc(var(--nav-h) + clamp(2rem, 8vw, 5rem)) clamp(3rem, 8vw, 5rem);
    overflow: hidden;
  }

  /* 배경 미디어 대신 은은한 방사형 글로우로 첫 화면 분위기를 만든다 */
  .hero::after {
    content: '';
    position: absolute;
    top: -30%;
    left: 50%;
    transform: translateX(-50%);
    width: min(52rem, 120%);
    aspect-ratio: 1;
    background: radial-gradient(
      circle,
      color-mix(in srgb, var(--accent) 16%, transparent) 0%,
      transparent 62%
    );
    pointer-events: none;
    z-index: 0;
  }

  /* 계기판 느낌의 격자 — 아래로 갈수록 사라짐 */
  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(var(--grid) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid) 1px, transparent 1px);
    background-size: 56px 56px;
    -webkit-mask-image: linear-gradient(to bottom, #000 0%, transparent 78%);
    mask-image: linear-gradient(to bottom, #000 0%, transparent 78%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.06em;
    color: var(--muted);
    margin-bottom: 1.25rem;
  }

  .pulse {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 0 var(--accent-soft);
    animation: pulse 2.6s ease-out infinite;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 var(--accent-soft);
    }
    70% {
      box-shadow: 0 0 0 8px transparent;
    }
    100% {
      box-shadow: 0 0 0 0 transparent;
    }
  }

  h1 {
    font-size: clamp(2.25rem, 7.4vw, 4.25rem);
    line-height: 1.18;
    letter-spacing: -0.035em;
    font-weight: 750;
    max-width: 16ch;
  }

  .lede {
    margin-top: 1.25rem;
    max-width: 46ch;
    color: var(--muted);
    font-size: clamp(1rem, 2.2vw, 1.1rem);
  }

  .metrics {
    margin-top: clamp(2.5rem, 6vw, 3.5rem);
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(11rem, 1fr));
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
    overflow: hidden;
  }

  .metrics li {
    padding: 1.15rem 1.25rem;
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .metrics li:last-child {
    border-right: 0;
  }

  .value {
    font-family: var(--font-mono);
    font-size: clamp(1.05rem, 2.7vw, 1.35rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--accent);
    line-height: 1.25;
  }

  .label {
    font-size: 0.8rem;
    color: var(--muted);
  }

  /* 2열로 접힐 때 격자선 재정렬 */
  @media (max-width: 46rem) {
    .metrics {
      grid-template-columns: repeat(2, 1fr);
    }
    .metrics li:nth-child(2n) {
      border-right: 0;
    }
    .metrics li:nth-child(-n + 2) {
      border-bottom: 1px solid var(--border);
    }
  }
</style>
